Bundled test dependency:

- `junit-4.7.jar`

Purpose:

- make automated tests reproducible from the repository itself;
- avoid depending on a machine-specific external JUnit installation.

This jar is used by:

- `nbproject/project.properties`
- `scripts/run-tests.ps1`
